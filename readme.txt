## npm:
       + --save-dev: nodemon
       + --save : express formidable ejs
       + built-in: path fs

## router:
       + /admin:
            /add-book
                .get : show form
                .post: save() and redirect to /shop/list
        
        + /shop:
            /list :
                / show all of books in file
                /toi-tai-gioi-ban-cung-the
                    show detail of book

